import { MatchDurationPipe } from './match-duration.pipe';

describe('MatchDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
